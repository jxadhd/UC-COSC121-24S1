def print_squares_to_number(number):
    """Takes int and prints all squares up to itself"""
    for i in range(1, number + 1):
        print(f'{i} * {i} = {i * i}')
    if number < 1:
        print("ERROR: number must be at least 1")
print_squares_to_number(0)